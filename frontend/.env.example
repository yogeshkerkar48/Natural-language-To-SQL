# API Configuration
# 
# By default, the app uses relative URLs (/api) which works with nginx proxy
# If you need to connect directly to backend without nginx, set:
# VITE_API_URL=http://localhost:8000/api
#
# For ngrok without nginx, use your ngrok backend URL:
# VITE_API_URL=https://your-backend-ngrok-url.ngrok.io/api
